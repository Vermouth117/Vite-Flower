import { memo } from "react";

import "./Order.css";

const Order = memo(() => {
  return (
    <main className="order">
      <h2 className="order-title">Order</h2>
    </main>
  );
});

export default Order;
